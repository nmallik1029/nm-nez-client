# badges

Drop badge pictures here. The file name is the badge's id: `owner.png` is the
badge that `src/shared/badges.ts` calls `owner`.

PNG, GIF or WEBP, 256 KB each at most, square and small. See the "badges/"
section of `assets/README.md` for the rest.
